type Animal {
  id: ID!
  animal_name: String!
  species: Species!
}

type Query {
  animals: [Animal]
}

type InputAnimal {
  animal_name: String!
  species_id: ID!
}

type Mutation {
  addAnimal(input: InputAnimal): Animal
}
